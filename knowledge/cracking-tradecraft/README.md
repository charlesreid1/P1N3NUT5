# cracking-tradecraft

Everything beyond `hashcat -m 22000 hs.22000 rockyou.txt`. Rules,
masks, hybrid attacks, SSID-derived wordlists, session ergonomics,
GPU tuning, distributed cracking. Read `hashcat/reference.md` first
for the mode catalog and the 22000 line format — this dir is the
"what to *do* with the mode" companion.

Companion: `hashcat/walkthrough.md`. This dir goes deeper.
